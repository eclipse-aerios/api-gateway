{{- if and .Values.krakend.tls.enabled (not .Values.krakend.tls.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "krakend.fullname" . }}-secret
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "krakend.labels" . | nindent 4 }}
type: Opaque
data:
  {{- with .Values.krakend.tls.certs }}
  cert.pem: {{ .cert | quote }}
  key.pem: {{ .key | quote }}
  {{- end }}
{{- end }}